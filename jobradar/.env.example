# jobradar needs only these two.
#
# In GitHub Actions they come from repository Secrets
# (Settings -> Secrets and variables -> Actions) and are never read from a file.
# Locally, copy this to .env and fill it in — .env is gitignored.
TELEGRAM_BOT_TOKEN=123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_CHAT_ID=000000000
